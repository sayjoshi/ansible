---

  - name: Touch file
    hosts: localhost
    gather_facts: true
    
    tasks:
      - name: Create the file
        file:
          path: /file5.txt
          state: touch
          mode: u+rw,g-wx,o-rwx
