---

  - name: Touch file
    hosts: localhost
    gather_facts: true
    
    tasks:
      - name: Create the file
        file:
          path: /file5.txt
          state: touch
          recurse: yes
          mode: 0755
